import { describe, it } from "mocha";

describe("Legacy bridge comprehensive suite", () => {
  it("is skipped because legacy provider bridge support has been removed", function() {
    this.skip();
  });
});
